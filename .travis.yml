language: node_js
node_js:
  - 16
services:
  docker
install:
  - yarn install
script:
  - npm run test
# deploy:
#   provider: script
#   script: bash deploy.sh
#   on:
#     branch: master
branches:
  only:
    - master
